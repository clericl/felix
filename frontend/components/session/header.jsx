import React from 'react';

const Header = props => {
    return (
        <header className="new-session-header">
            <Logo />
            <SessionForm />
        </header>
    )
};

export default Header;